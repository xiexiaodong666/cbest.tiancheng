spring:
  profiles:
    active: persist,dev
  application:
    name: e-welfare-merchant
server:
  port: ${tomcat.port:8080}

exclude-header-interceptor:
  - /swagger*/**
  - /doc.html*
  - /error*
  - /index
  - /**/*.css
  - /**/*.js
  - /**/*.png
  - /**/*.jpg
  - /**/*.jpeg
  - /**/*.ico